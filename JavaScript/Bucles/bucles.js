var contenedor = document.getElementById('contenedor')

// for (let i = 10; i <= 30; i = i + 5) {
//     contenedor.innerHTML += '<p class="rojo">' + i + '</p>'
// }

let vueltas = 10

// do {
//     console.log(vueltas);
//     vueltas = vueltas + 5
// } while (vueltas <= 30)

while(vueltas <= 30){
    console.log(vueltas);
    vueltas = vueltas + 5
}